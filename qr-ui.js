window.DFQR={identity(group,student){return JSON.stringify({type:'DF_CCF_IDENTITY',v:1,sessionId:group.id,groupId:group.id,studentId:student.id,last:student.last,first:student.first,classroom:student.classroom,sex:student.sex,project1:student.project1||'',project2:student.project2||''})},show(group,student){const raw=this.identity(group,student);navigator.clipboard?.writeText(raw);return raw}};